{% extends 'base.html.twig' %}

{% block title %}BoardSettings{% endblock %}

{% block body %}
    <h1>BoardSettings</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ board_setting.id }}</td>
            </tr>
            <tr>
                <th>Display_status_title</th>
                <td>{{ board_setting.displayStatusTitle ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_board_settings_index') }}">back to list</a>

    <a href="{{ path('app_board_settings_edit', {'id': board_setting.id}) }}">edit</a>

    {{ include('board_settings/_delete_form.html.twig') }}
{% endblock %}
